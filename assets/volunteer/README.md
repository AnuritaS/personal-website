# Volunteer photos

Drop photos here named `volunteer-1.jpg`, `volunteer-2.jpg`, `volunteer-3.jpg`
(etc.) to fill the volunteer cards on the Extracurricular page. Any slot
without a matching file automatically shows a soft placeholder instead of
a broken image — add or remove `.volunteer-card` blocks in
`extracurricular.qmd` to match how many roles you want to show.
