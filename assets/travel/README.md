# Travel photos

Drop photos here named `travel-1.jpg` through `travel-6.jpg` to fill the
gallery on the Travel page. Any slot without a matching file automatically
shows a soft placeholder tile instead of a broken image — add fewer or
more by editing the `<div class="travel-photo">` blocks in `travel.qmd`.
